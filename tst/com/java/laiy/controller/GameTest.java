package com.java.laiy.controller;

import org.junit.Test;

import static org.junit.Assert.*;

public class GameTest {

    @Test
    public void testTheGame() throws Exception {
        //TODO
        /*
                final String GAME_NAME = "XO";
        final String expectedValueO = "Oleg";
        final String expectedValueX = "Xonstantin";
        final Board board = new Board();
        final Player[] players = new Player[2];
        players[0] = new Player("Xonstantin", Figure.X);
        players[1] = new Player("Oleg", Figure.O);
        final GameController game = new GameController(GAME_NAME, players, board);
        final ConsoleView consoleView = new ConsoleView(game);
        Figure testValueO = Figure.O;
        Figure testValueX = Figure.X;
        board.setFigure(0,1,testValueO);
        board.setFigure(1,2,testValueO);
        board.setFigure(0,0,testValueX);
        board.setFigure(2,2,testValueO);
        board.setFigure(2,0,testValueO);
        board.setFigure(0,2,testValueX);
        board.setFigure(1,0,testValueX);
        board.setFigure(1,1,testValueX);
        //board.setFigure(2,1,testValueX);
        consoleView.showDraw();
        assertEquals("Draw!" + "\n", outContent.toString());
         */
    }
}